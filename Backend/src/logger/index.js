const logger = require("./logger");
const { logWithMeta, logInfo, logWarn, logError, logDebug } = require("./helpers");

module.exports = {
  logger,
  logWithMeta,
  logInfo,
  logWarn,
  logError,
  logDebug
};